#!/bin/bash
if [ -z "$1" ]; then
    echo "need to specify virtualenv name";
    exit 1;
fi
ln -s ~/.autoenv_default .env
echo "$1" >.virtualenv
echo ".env" >>.gitignore
echo ".virtualenv" >>.gitignore
git add .gitignore
git commit -m "ignore virtualenv files"
