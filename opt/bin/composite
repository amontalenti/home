#!/bin/bash
function use_metacity() {
	metacity --replace &
	sleep 1;
}

function use_compiz() {
	killall metacity;
	sleep 1;
	compiz &
}

if pidof compiz; then
	use_metacity;
elif pidof metacity; then
	use_compiz;
else
	use_metacity;
fi
